
export interface Info {
    name:    string,
    score:   string,
    star:    string,
    perpect: string
};

export interface SupportInfo {
    id: number,
    name: string,
    content: string
};

export interface PerpectInfo {
    id: number,
    name: string,
    select_id: number
};